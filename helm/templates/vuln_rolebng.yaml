apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.vuln.roleBinding.name }}
  namespace: {{ .Values.vuln.roleBinding.namespace }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.vuln.roleBinding.subjects.name }}
    namespace: {{ .Values.vuln.roleBinding.subjects.namespace }}
roleRef:
  kind: Role
  name: {{ .Values.vuln.roleBinding.roleRef.name }}
  apiGroup: rbac.authorization.k8s.io