apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sast.service.name }}
  namespace: {{ .Values.sast.service.namespace }}
spec:
  selector:
    tier: {{ .Values.sast.service.spec.selector.tier }}
  {{- $port := index .Values.sast.service.spec.ports 0 }}
  ports:
    - port: {{ $port.port }}
      targetPort: {{ $port.targetPort }}
      protocol: {{ $port.protocol | default "TCP" }}
