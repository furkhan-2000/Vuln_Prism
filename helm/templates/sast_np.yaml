apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.sast.networkPolicy.name }}
  namespace: {{ .Values.sast.networkPolicy.namespace }}
spec:
  podSelector:
    matchLabels:
      tier: {{ .Values.sast.networkPolicy.podSelector.tier }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - ipBlock:
            cidr: {{ .Values.sast.networkPolicy.ingress.ipBlock.cidr }}
      ports:
        {{- range .Values.sast.networkPolicy.ingress.ports }}
        - protocol: {{ .protocol | default "TCP" }}
          port: {{ .port }}
        {{- end }}
  egress:
    - to:
        - ipBlock:
            cidr: {{ .Values.sast.networkPolicy.egress.ipBlock.cidr }}
      ports:
        {{- range .Values.sast.networkPolicy.egress.ports }}
        - protocol: {{ .protocol }}
          port: {{ .port }}
        {{- end }}